This site is prepared to use a Google Apps Script web app as the quote backend.

For the $0 setup, deploy apps-script/Code.gs as a Google Apps Script web app and paste its /exec URL into the CONFIG block in index.html where QUOTE_ENDPOINT is defined.
